fix(onboarding): 19 UX robustness fixes for onboarding screens

Comprehensive frontend fixes across 8 files covering positioning,
accessibility, validation, performance, and edge cases:

- InlineSearchableCombobox: add position:relative to parent containers,
  use React.useId() for unique aria-controls/aria-labelledby IDs,
  add Escape key handler to close dropdown, prevent CLS with invisible
  badge container, support getSelectedLabel for custom value display
- SearchableCombobox: same CLS fix and getSelectedLabel support
- Schemas: align fullName regex with server (allow hyphens/apostrophes),
  remove restrictive headline regex, extend headline max to 200
- DisplayName: fix regex from + to * to allow empty optional field
- Location: show inline format error instead of clearing user input
- Company field: remove required validation, match Zod optional
- Stepper: dynamic connecting line positioning via step count
- SessionStorage: debounce form persistence with useDebounce(800ms)
- Skip & Complete: only show on steps 3+ where data exists
- Completion: calculate percentage from actual form data entered
- Email: fix verification check to use !!(truthy) pattern
- DisplayName auto-extraction: pass onNameExtracted callback from page
- FullName: guard against async fetchUser overwriting user input
- Role suggestions: detect from headline text instead of dead role field
- Drag target: add ring indicator for drop target position
- Double-click: add isTransitioning guard to prevent step skipping

Co-authored-by: CommandCodeBot <noreply@commandcode.ai>
